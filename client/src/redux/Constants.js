const USER_TYPE = {
    Student: "Student",
    Teacher: "Teacher"
};

const DATE_TIME_FORMAT = "MM-DD-YYYY HH:mm:ss";

const ReduxContents = {
    USER_TYPE,
    DATE_TIME_FORMAT
};

export default ReduxContents;