const USER_TYPE = {
    Student: "Student",
    Teacher: "Teacher"
};
const DATE_FORMAT = "MM-DD-YYYY"
const DATE_TIME_FORMAT = `${DATE_FORMAT} HH:mm:ss`;

const ReduxContents = {
    USER_TYPE,
    DATE_TIME_FORMAT,
    DATE_FORMAT
};

export default ReduxContents;