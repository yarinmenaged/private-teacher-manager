const TimeFormat = 'HH:mm';
const DATE_FORMAT = 'DD/MM/YYYY';

const USER_TYPE = {
    Student: "Student",
    Teacher: "Teacher"
};

const EventConstants = {
    TimeFormat,
    USER_TYPE,
    DATE_FORMAT
};

module.exports = EventConstants;