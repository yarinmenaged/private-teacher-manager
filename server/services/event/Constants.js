const DATE_WITH_TIME_FORMAT = 'MM-DD-YYYY HH:mm:ss';
const DATE_FORMAT = 'MM-DD-YYYY';

const EventServiceConstants = {
    DATE_WITH_TIME_FORMAT,
    DATE_FORMAT
};

module.exports = EventServiceConstants;